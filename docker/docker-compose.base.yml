version: '3.7'

services:
  base:
    image: vvd-env
    container_name: vvd-base
    build:
      context: .
      dockerfile: env-setup.Dockerfile
    volumes:
      - ../:/vvd
      - ~/.m2:/root/.m2
    working_dir: /vvd